# Workflows Package Override

Applies inside `src/trading_bot/workflows/`.

## Folder Meaning
1. `workflows` is the orchestration layer.
2. It combines multiple steps into higher-level repeatable runs.
3. Keep this folder focused on coordination rather than low-level transformation rules.
