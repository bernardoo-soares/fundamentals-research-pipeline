# Contracts Package Override

Applies inside `src/trading_bot/contracts/`.

## Folder Meaning
1. `contracts` is the schema and interface definition layer.
2. It defines the shapes, field sets, and validation rules used by the rest of the system.
3. Keep these definitions stable, explicit, and independent from orchestration flow.
