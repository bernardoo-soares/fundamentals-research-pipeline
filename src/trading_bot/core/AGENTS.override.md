# Core Package Override

Applies inside `src/trading_bot/core/`.

## Folder Meaning
1. `core` is the shared infrastructure layer.
2. It holds generic runtime concerns used across the package.
3. Keep this folder provider-agnostic and reusable by higher layers.
